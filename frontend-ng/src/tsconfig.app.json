{
    "extends": "../tsconfig.json",
    "compilerOptions": {
        "outDir": "../out-tsc/app",
        // Have to specify node types for cognito js to compile
        "types": ["node"]
    },
    "exclude": [
        "test.ts",
        "**/*.spec.ts"
    ]
}
