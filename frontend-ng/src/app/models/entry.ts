import { EncryptedEntity } from './encrypted-entity';

/**
 * Representation of an Entry
 */
export interface Entry extends EncryptedEntity {
    // Entry does not implement anything beyond EncryptedEntity.
}
