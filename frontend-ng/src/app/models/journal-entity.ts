import { EncryptedEntity } from './encrypted-entity';

/**
 * Representation of a Journal
 */
export interface JournalEntity extends EncryptedEntity {
    // JournalEntity does not implement anything beyond EncryptedEntity.
}
